# darkside 0.0.9000

* Limiting dependencies to xml2
* Added support for additional X columns in start end survival tables (XAdvancedColumn)
* Added support for subcolumn names used in grouped tables with already calculated errors
* x_value column is renamed if a title is provided in the table
* Added write_pzfx function to export data frames (for now supports only column table types _i.e._ wide format)
